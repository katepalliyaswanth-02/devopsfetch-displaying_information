# Core system utilities
psutil==6.0.0           # For CPU, memory, ports, and process info
tabulate==0.9.0         # For pretty table output
rich==13.7.1            # For colorful terminal formatting (optional, nicer output)

# For parsing and working with system logs
python-dateutil==2.9.0
humanfriendly==10.0     # For readable time and size formatting

# For working with subprocesses (Docker, Nginx, etc.)
docker==7.1.0           # To interact with Docker containers/images programmatically

# For log management and rotation
loguru==0.7.2           # Elegant logging with rotation support
